---
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Hero from "@/components/Hero.astro";
import Footer from "@/components/Footer.astro";
import CaseStudyCard from "@/components/CaseStudyCard.astro";
import { getAllCaseStudies } from "@/lib/notion";

const caseStudies = await getAllCaseStudies();
---

<Layout title="Jérémie Taubes — Portfolio">
  <Header />
  <Hero />

  <section id="work" class="px-10 pt-18 pb-24" style="scroll-margin-top: 116px;">
    <div class="max-w-card-list mx-auto">

      <!-- Cards container -->
      {caseStudies.length === 0 ? (
        <p class="text-sm text-ink-muted">Aucun projet publié pour le moment.</p>
      ) : (
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
          {caseStudies.map((cs, i) => (
            <CaseStudyCard
              title={cs.title}
              description={cs.summary}
              tags={cs.tags}
              client={cs.client}
              year={cs.year}
              coverImage={cs.coverImage}
              slug={cs.slug}
              index={i}
              location={cs.location}
            />
          ))}
        </div>
      )}
    </div>
  </section>

  <Footer />
</Layout>
